version: '2'

services:
  users-module:
    build:
      context: ./
      dockerfile: ./Dockerfile
    container_name: users-module
    command: pm2 start
    ports:
      - "7070:7070"
    environment:
      - NODE_ENV=production
      - IP=0.0.0.0
      - PORT=7070
      - LOGTYPE=dev
